# Environment Variables Template
# Copy this file to .env and configure for your environment

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Connection
# Option 1: Local MongoDB (requires MongoDB installed)
MONGODB_URI=mongodb://localhost:27017/mangaverse

# Option 2: MongoDB Atlas (cloud database - recommended for sharing)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mangaverse?retryWrites=true&w=majority

# Session Secret (MUST change this to a random string)
SESSION_SECRET=mangaverse-secret-key-change-in-production

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. If using MongoDB Atlas:
#    - Create free account at https://www.mongodb.com/cloud/atlas
#    - Create a cluster
#    - Get connection string and replace MONGODB_URI
# 3. If using local MongoDB:
#    - Install MongoDB: https://www.mongodb.com/docs/manual/installation/
#    - Start MongoDB: sudo systemctl start mongod (Linux) or brew services start mongodb-community (Mac)
# 4. Change SESSION_SECRET to a random string
# 5. Save the file and run: npm install && npm run seed && npm start
